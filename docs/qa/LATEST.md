# Latest QA Regression

- Current report: [QA_REGRESSION_REPORT.md](QA_REGRESSION_REPORT.md)
- **Run automated checks:** `bash docs/qa/run_checks.sh` (from main repo path — see §3c canonical execution locus rule)
- Date (UTC): 2026-05-12 (Task 3 + Landing page audit + Matrix-QC upgrade)
- Tag: **v3.0.10** (GA, latest, shipped 2026-05-08) — worktree edge case governance: §1 worktree fallback for skip-worktree files (`dev/SESSION_HANDOFF.md` / `dev/SESSION_LOG.md` filtered out of worktree checkout) + §3c canonical execution locus for harness (worktree-path execution triggers spurious `H01` / `R27-10` failures by-design per skip-worktree convention; main repo path is canonical). Plus combined zh-TW LOW residual sweep (README.zh-TW.md `入面` / `令` / `用戶` tokens at v3.0.8 row converged to formal-written register matching v3.0.9 sweep policy). Post-tag polish (2026-05-09): README 4-locale Repository source layout / Core files structural completion (補 v3.0.7+ wizards/templates/RUNBOOK references) + zh-TW Cantonese register sweep + §0b local-tool / SDK / skill alignment principle extension (post-tag, R33-48/49 +2 checks) + 2026-05-12 Task 2 hardening (Pre-action discipline + File proliferation + Closeout stray-file scan, R33-50..57 +8 checks) + 2026-05-12 Task 1 Language layer separation framework (non-developer default + 3-layer framework + multilingual universality, R33-58..65 +8 checks)
- Result: 400 total automated checks (311 main + 89 legacy auto-chain), 400 pass, 0 fail
- Latest patch focus (2026-05-12 Landing page audit + Matrix-QC upgrade — reflects Task 1 / Task 2 / Task 3 across user-facing site + internal audit framework): Landing page (`docs/site/index.html`) audit added 2 new feature cards (f13 External Knowledge Surface + f14 Language Register Discipline) covering Task 3 §10b and Task 1 three-layer framework user-facing reflection, updated f6 (File Safety Governance) description to include Task 2 hardening content (Pre-action three-step verify + Closeout stray-file scan), added scenario sc4 (Notes Live in Notion or Obsidian) covering Task 3 use case, added FAQ entry covering external KB workflow, in all 4 locales (EN / zh-TW / zh-CN / ja). Stat counter `data-target` 396 → 400. Matrix-QC agent (`.claude/agents/matrix-qc.md`, gitignored) upgraded from 10-dimension to 13-dimension audit: added Dim 11 External Knowledge Surface integrity (audits §10b mirror parity + 8 cross-cutting integration consistency + DOC_SYNC row + supporting files), Dim 12 Reply register discipline integration (audits §11a rule 5 three-layer framework completeness + multilingual neutrality), Dim 13 Pre-action discipline integrity (audits §3 READ 6-item + §3b File proliferation + §4 stray-file scan); extended Dim 10 generality with external KB cookbook tool-neutrality hard rule check; SSOT scope extended from 8 to 10 files (added `dev/CODEBASE_CONTEXT.md` for Dim 11, `dev/wizards/playbook.md` for Dim 11 + Dim 13). Harness R33-88..91 (+4 checks) for landing page i18n parity (f13 / f14 present + f13_t / sc4_t in 4 locales); EXPECTED_INDEX_COUNTER 396 → 400. Root cause addressed: landing page lagged behind three Task land (only `data-target` was updated; feature cards / scenarios / FAQ did not reflect new functionality); matrix-qc audit framework also lagged (no audit dimensions covering external KB / register discipline / pre-action discipline introduced this session).
- Prior patch focus (2026-05-12 Task 3 — External Knowledge Surface governance §10b new section + 8 cross-cutting integrations): §10b new section covers external knowledge surface (Notion / Obsidian / Google Drive / similar tools) integration into governance workflow — trigger conditions, two access modes (Mirror / Bridge), two AI access variants (Direct via MCP-API-sync / Paste-only), Cloud-side destructive op safety, Mode switch protocol, Tool neutrality hard rule. Cross-cutting integration: §1 startup conditional read for `dev/EXTERNAL_KB.md` pointer; §2 SoT priority new rule 7 placing Bridge-mode external content at same level as PROJECT_MASTER_SPEC; §3 PLAN onboarding readiness check adds external KB trigger; §3 PERSIST adds external KB sync step (direct-access executes / paste-only surfaces ready-to-paste block); §4 closeout adds external KB sync check before smart-skip gate (Bridge-mode scopes cannot leave un-synced unless user explicitly defers); §3.6 wizard system adds external KB variant + PROFILE.md schema adds wizard_disabled_external_kb flag; §5a backup list adds 3 new files. New files: `dev/templates/external_kb_template.md` (sample pointer with 8 properties), `docs/EXTERNAL_KB_COOKBOOK.md` (tool-specific best practice reference for Notion / Obsidian / Google Drive / Apple Notes / Logseq-Roam-Anytype; explicitly reference not mandate). Existing support files updated: `dev/wizards/playbook.md` adds Variant — External KB wizard section; `dev/wizards/README.md` adds template entry + PROFILE.md schema flag; `dev/CODEBASE_CONTEXT.md` adds External Knowledge Surface section template; `dev/DOC_SYNC_CHECKLIST.md` adds new row for external KB setup changes. INIT.md install POST-INSTALL message extended from 2 to 3 separate messages (added Message 3 — external KB setup prompt with 3-path decline). Harness R33-70..87 (+18 checks, 9 mirror parity pairs) AGENTS↔INIT; EXPECTED_INDEX_COUNTER 378 → 396. Root cause addressed: external knowledge tools (which many users maintain in parallel to local repo) were not integrated into governance workflow before — AI could miss persist-sync at PERSIST and at closeout, causing drift between local and external. §10b now ensures startup reads, PERSIST writes, and closeout sync checks all include external surface when configured.
- Prior patch focus (2026-05-12 Task 1 third batch — register signals disambiguation): §11a rule 5 框架第一層措辭由 `everyday language matching the user's chat language` 明確化為 `formal-but-conversational written register matching the user's chat language AND the actual register signals from user's messages` — 明確聲明對齊用戶實際樣本,而非 AI 預設語體;發送前自我審查由 1 個 scan 拆為 2 個 scan:Scan 1 既有 banned-pattern-as-subject scan 保留,Scan 2 新加「User-AI register mismatch detection」— 比對 AI 草稿與用戶最近 3 條訊息的 register signals(vocabulary register、dialect markers、sentence-fragment vs. complete-sentence、inline foreign-language technical terms),不一致則重寫;正例與反例段落由單一方言粵語加中英夾雜重寫為 4 種語言並列示例(英文 / 繁體書面 / 簡體書面 / 日文),明確聲明示範性,同一原則適用任何自然語言。Harness R33-66..69(+4 checks)mirror parity AGENTS↔INIT;EXPECTED_INDEX_COUNTER 374 → 378。Root cause addressed:用戶於 2026-05-12 反饋指出「混血兒式」回覆風格屬最希望解決問題;經三層反思(meta instruction / memory / framework)發現含糊詞「everyday language / 日常話」由上層指令穿透至記憶再至框架,且框架正例自身違規。範圍拆分原則:公眾治理只做通用 register matching 紀律強化(多語言中立),粵語特定 ban list 與中英夾雜技術詞對應表只入私人 CLAUDE.md 與全域用戶記憶,不污染多語言通用原則。
- Prior patch focus (2026-05-12 Task 1 Language layer separation framework — §11a rule 5 重構): §11a rule 5 由單 paragraph 重構成三層 framework。Opening explicit default — `Assume the user may not be a software developer`,Layer 1 scope cover 6 種 chat surface (conversation / choice prompts / summaries / explanations / conclusions / next-step suggestions);commit message / code comment / governance file internal text 屬 Layer 2 不受 Layer 1 紀律約束 — explicit 劃 chat-vs-internal 邊界。Layer 2 governance internal naming 加 explicit file scope enumeration (`AGENTS.md` / `INIT.md` / `dev/*` / `docs/qa/*` / `docs/releases/*` internal-only),容許英文 anchor (§ codes / FPFR / SSOT 等) 內部 reference,出現 Layer 1 必須翻譯。Layer 3 governance internal schema files (`dev/SESSION_HANDOFF.md` / `dev/SESSION_LOG.md` / `dev/CODEBASE_CONTEXT.md` / `dev/PROJECT_MASTER_SPEC.md`) 必須英文 schema (cross-AI agent 互操作必要);user-facing surface files (README + 本地化變體 / landing page / 公開 release notes) 跟用戶 locale;harness anchor / automation grep pattern 跨層豁免。多語言通用聲明 + 3 locale (EN / 中 / ja) concrete example 證明 framework 跨自然語言通用 (`applies to any natural language`,適用 ko / es / fr / ar / hi / vi / th / etc.)。Rule 9 (user-supplied schema verbatim alignment) + Rule 10 (reply register consistency) 保留獨立 rule (wording 不變),framework 段尾 cross-reference 提及。Banned-as-sentence-subject patterns + Ground truth identifiers allowed inline + Pre-ship self-check + Violation handling 既有 wording reframe 入 Layer 1 (key phrases `do not weave English mid-sentence` + `Ground truth identifiers allowed inline` reframe-preserve,R33-28..31 既有 anchor 仍 hit)。Harness R33-58..65 (+8 checks) mirror parity AGENTS↔INIT;EXPECTED_INDEX_COUNTER 366 → 374。Root cause addressed: 既有 Rule 5 wording 偏 abstract,cold-start AI 易漏睇 / 鬆懈;explicit framework 令 future session AI 一打開 governance file 就見清楚紀律 + 多語言通用 explicit 化保護廣大非中 / 英語 user。
- Prior patch focus (2026-05-12 Task 2 hardening — Pre-action discipline + File proliferation + Closeout stray-file scan): §3 READ minimum coverage extended from 4 to 6 items — (a) files not Read in this session are treated as `UNVERIFIED` per §11a rule 4 until Read (mirrors §0b's external-API rule applied to internal context); (b) before creating a new file, three-step verify that no existing file can be modified to serve the same purpose (list related directories / grep same-concept keywords / review recent SESSION_LOG entries). §3b adds `File proliferation discipline` sub-section — named anti-pattern *Throwaway / orphan file*, forbidden scratch / temp / `_old` / `_tmp` / dated-session-suffix names in committed work, every kept file must have stable purpose recorded in CODEBASE_CONTEXT.md Directory Map or referenced in SESSION_LOG.md. §4 closeout adds `Closeout stray-file scan` mandatory action — scan `dev/` and repo root for files not in Directory Map and not gitignored before evaluating smart-skip gate, three disposition paths (integrate / confirm deletion / record rationale). Harness R33-50..57 (+8 checks) mirror parity AGENTS↔INIT; EXPECTED_INDEX_COUNTER 358 → 366. Root cause addressed: AI草率新建檔案 → next-session orphan → repo bloat pattern reported by user (file proliferation痛點), plus AI 憑記憶推測 internal 結構嘅 risk previously only covered for external API.
- Prior patch focus (2026-05-09 §0b local-tool alignment extension): §0b 第 2 段尾追加 1 段 — `Same alignment principle applies to local CLI tools, installed SDKs, package-manager invocations, and project-level skills: prefer documented sources (project SSOT > skill description / runbook > --help / --version self-check > official docs) over training-memory syntax. When uncertainty remains about a flag, parameter, or skill behavior, flag it as UNVERIFIED per §11a rule 4 before invoking — do not silently fall back to training-memory guesses.` Reuse 既有 §11a rule 4 `UNVERIFIED` 機制,non-breaking;harness R33-48/49 mirror parity AGENTS↔INIT;EXPECTED_INDEX_COUNTER 356 → 358 (+2 actual,FPFR plan 估 +1 但 既有 R33-42..47 split AGENTS/INIT pattern consistency 優先,minor execution detail divergence)。
- Prior patch focus (2026-05-08 worktree edge case governance for v3.0.10): (a) §1 `Worktree fallback (mandatory)` added — Inside a git worktree, `dev/SESSION_HANDOFF.md` and `dev/SESSION_LOG.md` may be filtered out of the worktree's working tree (skip-worktree convention used to keep these local-only state files out of git history); when a worktree session starts and these files are absent in the worktree path, AI must read them from the main repo path (resolve via `git rev-parse --git-common-dir` parent dir, or by inspecting the current path for `.claude/worktrees/`) before falling back to the "create a minimal version" rule. The "create a minimal version" rule applies only when both worktree and main repo lack the files (genuinely missing, not skip-worktree filtered). (b) §3c Phase 1 step 2 `Canonical execution locus` sub-bullet added — harness must run from the main repo path; worktree-path execution triggers spurious harness failures (`H01` `.legacy_last_run` cross-tree drift, `R27-10` skip-worktree files absent in worktree) that are by-design per skip-worktree convention, not real failures; AI must explicitly cd to main repo path before invoking the harness during release verification. (c) Combined zh-TW LOW residual sweep — README.zh-TW.md `入面` / `令` / `用戶` tokens at v3.0.8 row converged to formal-written register matching v3.0.9 sweep policy. Harness R33 series extended (R33-42..47, +6 checks): §1 Worktree fallback parity AGENTS↔INIT (R33-42/43), §3c Canonical execution locus parity AGENTS↔INIT (R33-44/45), skip-worktree convention reference parity AGENTS↔INIT (R33-46/47). EXPECTED_INDEX_COUNTER 350 → 356.
- Prior patch focus (2026-05-07 reply-tone hard-rule patch + work-pool boundary patch in v3.0.9, both requested by user same day): **Round 1 — reply-tone hard rule:** (a) §11a rule 5 plain-language strengthen — Chinese-context inline-English-mixing ban (English appears only for proper nouns / established terms with no clean translation / parenthetical traceability tags); explicit governance internal ID list (§ codes / D-XXX / F-XXX / priority #X / Track A/B/C / Phase #N / Reflection Follow-Through Protocol / Capital_Snake_Case names) banned from sentence subjects in conversational reply; ground truth carve-out (file path / git SHA / version number / commit hash / function / variable / command names from actual code) explicit allow inline. (b) §11a rule 2 outcome-first option framing — every choice option's label must lead with impact / outcome / what it means for the user's goal, not with mechanism (file changed, how it's done, tech). (c) Header startup inventory ghost-reference fix — §0a Layer Separation added to startup inventory (line 5); §0a hard rules are always-applicable but were missing from inventory. (d) §5a bootstrap backup list ghost-reference fix — `dev/wizards/playbook.md` / `dev/wizards/README.md` / `dev/templates/spec_template.md` / `dev/templates/runbook_template.md` added to backup list (v3.0.7+ governance files were missing, would not be backed up on re-install). (e) Cross-doc baseline drift cleanup — Snapshot status grep-anchor count synced from outdated 290 (pre-R33) → 325 (post-R33×35); LATEST.md / QA_REGRESSION_REPORT.md / VERIFICATION.md narratives bumped from 336 to 350. **Round 2 — work-pool boundary:** (f) §1 `Work-pool boundary (mandatory)` added — handoff prompt block is *seed context only*; its description of prior-session done / changed / pending is HISTORY, not part of this session's work pool; PLAN must distinguish prior-session record (read-only state context) from this session's actual work pool (derived from user's current request + files actually opened/edited/created in THIS conversation). (g) §4 `Session work-pool boundary (mandatory)` added — SESSION_LOG entry's Completed / Changed / Done / Files-this-session-edited fields strictly reflect work *actually performed in THIS conversation*; cross-session continued tasks record only the increment (`continued from prior session: <delta>`), not cumulative completion. Cross-reference between §1 and §4 enforces hard separation at both endpoints. Root cause addressed: old INIT.md users reported recurring SESSION_LOG drift (prior-session done items copy-pasted into this session record) — prior governance left work-pool boundary to mental tracking, which drifted in practice. Harness R33 series extended (R33-28..37 round 1, R33-38..41 round 2, +14 checks total) — mirror parity for AGENTS↔INIT on each new hard rule + ghost-reference fix; EXPECTED_INDEX_COUNTER 336 → 350.
- Focus of v3.0.8 release (historical reference; see `docs/releases/v3.0.8.md`): paradigm response patch triggered by first real-install dogfood feedback. (1) INIT.md `POST-INSTALL: Profile Selection` 6 options reflowed inline → markdown list. (2) Install flow split into two separate messages — Message 1 declares `Governance framework ready`, Message 2 offers optional wizard — removing same-screen confusion between setup completion and wizard prompt. (3) Wizard Step 1 in `dev/wizards/playbook.md` reframed from "1-sentence cold question" to "main question + 3 optional supplements (📁 reference files / 🔗 URLs / 📌 known decisions) + explicit escape phrase". (4) Source-grounding discipline added — AI actively reads provided sources (Read for paths / WebFetch for URLs / grep for keywords) before drafting; silent fabrication banned. (5) Labeled assumption list — every assumption tagged `[from your input]` vs `[my inference]`. (6) Vague-input fallback escape hatch reframed user-language-agnostic. (7) 4 prior Chinese-mixed-into-English defects cleaned. (8) Hardcoded tech-stack example in playbook generalized. Harness R33-21..27 (+7 checks); EXPECTED_INDEX_COUNTER 329 → 336.
- Focus of v3.0.7 release (historical reference; see `docs/releases/v3.0.7.md`): (1) Wizard paradigm shift — form-fill schemas (`_visual_frame.md` / `spec_starter.md` / `runbook_starter.md`) retired; new `dev/wizards/playbook.md` behavior layer + `dev/templates/` content namespace (spec + runbook templates); user supplies 1-sentence project description, AI drafts full file + assumption list, iterates, proposes write. (2) Matrix-QC audit-logic root-fix — `.claude/agents/matrix-qc.md` Boundary-Aware Divergence section + Hard rule #2 prescriptive-verb ban; audits stay description-only. (3) Playbook dogfood patches — Step 4 closure classification (soft vs explicit write), Step 2 hallucination guardrail with `(待補)`, Step 2 explicit per-field assumption discipline. (4) README × 4 locale heading drift fix (line 248 in zh-TW / zh-CN / ja). (5) Landing page f12 feature card surfacing wizard system in 4 languages. (6) Harness R33 series rework 17 → 14 checks, EXPECTED_INDEX_COUNTER 332 → 329
- Prior v3.0.6 supplement focus: Recent releases version table capped to 5 entries; 5-step lifecycle text + Quick Op 1)/2) commands aligned with SVG canonical; "Forgot to paste" callout corrected with model-specific reliability (~40-85% range); LATEST_STABLE_TAG="v3.0" → "v3.0.6" so R29 catches per-patch row drift; DOC_SYNC "Release published" row strengthened
- Original v3.0.6 GA focus: 6 redesigned Visual Cues (Boot ✧/⚓/●; Closeout 🌅/🌙/🏁); "paste hint" simplified; README Install + Upgrading restructured to 5-step flow; Quick Op 3) explains why pasting beats short trigger; R27-10 bash exit-code capture bug fixed
- Sandbox install QC: 3 HIGH-risk scenarios verified at v3.0.2 baseline; v3.0.5 + v3.0.6 changes are governance text + README polish only — sandbox re-validation deferred per §3c Phase 3 discretionary cleanup for non-mechanism releases
- Matrix QC audit reference: 10-dimension audit on prior commits resolved at v3.0.3 release
